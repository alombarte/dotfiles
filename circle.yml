machine:
  ghc:
    version: cabal-install-1.18

dependencies:
  post:
    # install shellcheck
    - cabal update; cabal install shellcheck

test:
  override:
    - PATH="$HOME/.cabal/bin:$PATH" ./test.sh
